name: Sync Files
on:
  push:
    branches:
      - master
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@master
      - name: Run GitHub File Sync
        uses: Leocete/repo-file-sync-action@latest
        with:
          GH_PAT: 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